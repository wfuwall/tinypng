# tinypng

压缩图片的工具包

### 使用方式

- 安装

```js
npm install @didi/tinypng -D
```

- 执行命令

```js
tinypng --input src/assets --output client/tinypng
```

> --input 指定要压缩的目录，--output 指定压缩文档和图片指纹的输出目录，压缩后的图片会默认替换原图片
